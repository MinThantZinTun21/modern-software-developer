# modern-software-developer

TypeScript prompt-chaining agent using the Anthropic SDK through OmniRoute.

```bash
cd ts-code-agent
cp .env.example .env   # add your OMNIROUTE_API_KEY
npm install
npm run dev
```
